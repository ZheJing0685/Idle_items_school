# 问题修复报告：卖家物品管理逻辑问题

## 问题描述
当买家购买物品后，物品处于待发货阶段，但卖家这边无法对物品进行后续操作，只能对物品进行上架和删除操作。

## 问题分析

### 核心问题
1. **状态机不完整**：物品状态变为 SOLD 后，缺少与订单状态的联动
2. **功能缺失**：卖家无法在物品管理页面查看关联订单信息
3. **操作限制缺失**：有活跃订单时仍允许上架/删除操作

### 代码定位
- `frontend/src/views/user/Items.vue`：操作按钮逻辑过于简单
- `backend/src/main/java/com/idleitems/school/service/OrderService.java`：缺少按物品ID查询订单的方法
- `backend/src/main/java/com/idleitems/school/controller/ItemController.java`：缺少获取物品关联订单的接口

## 解决方案

### 后端修改

#### 1. OrderRepository.java
新增方法：
- `findByItemIdAndOrderStatusIn()`：按物品ID和订单状态查询
- `findByItemIdAndSellerId()`：按物品ID和卖家ID查询

#### 2. OrderService.java
新增方法：
- `getOrdersByItemId()`：获取物品关联的所有订单
- `getActiveOrdersByItemId()`：获取物品关联的活跃订单

#### 3. ItemController.java
新增接口：
- `GET /api/items/{id}/orders`：获取卖家物品关联的订单
- `GET /api/items/{id}/active-orders`：获取物品关联的活跃订单

### 前端修改

#### 1. API 路径配置 (paths.js)
新增路径：
- `ITEM.ORDERS(id)`：物品关联订单路径
- `ITEM.ACTIVE_ORDERS(id)`：物品活跃订单路径

#### 2. API 服务 (item.js)
新增方法：
- `getItemOrders()`：获取物品关联订单
- `getItemActiveOrders()`：获取物品活跃订单

#### 3. 用户物品管理页面 (user/Items.vue)
**新增功能**：
1. **订单信息展示**：已售出物品显示关联订单的详细信息
   - 订单号
   - 买家信息
   - 下单时间
   - 订单状态

2. **智能操作按钮**：
   - **发货按钮**：仅在待发货订单时显示
   - **查看订单按钮**：有订单时显示，跳转到订单列表
   - **编辑按钮**：有活跃订单时禁用
   - **上架/下架按钮**：有活跃订单或审核中时隐藏
   - **删除按钮**：有活跃订单时隐藏

3. **状态判断逻辑**：
   - `canEdit()`：判断是否可以编辑
   - `canToggleShelf()`：判断是否可以上下架
   - `canDelete()`：判断是否可以删除

#### 4. 样式文件 (user-items.css)
新增订单信息展示样式：
- 订单信息卡片样式
- 订单详情布局样式
- 响应式适配

## 状态流转说明

### 物品状态机
```
DRAFT -> PENDING -> ON_SALE <-> OFF_SHELF
                      ↓
                    SOLD（支付后）
                      ↓
                    ON_SALE（退款完成后，若无其他活跃订单）
```

### 卖家操作规则

| 物品状态 | 订单状态 | 可用操作 |
|---------|---------|---------|
| DRAFT | - | 编辑、删除 |
| PENDING | - | 查看详情 |
| ON_SALE | - | 编辑、下架、删除 |
| OFF_SHELF | - | 编辑、上架、删除 |
| SOLD | PENDING_SHIPMENT | 发货、查看订单 |
| SOLD | SHIPPED | 查看订单 |
| SOLD | REFUND_REQUESTED | 查看订单 |
| SOLD | COMPLETED | 查看订单 |

## 测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 前端构建 | 通过 | vite build 成功 |
| 后端代码语法 | 通过 | Java 代码语法正确 |
| API 路径定义 | 通过 | 新增路径格式正确 |
| 前端 API 调用 | 通过 | 新增方法语法正确 |

## 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `backend/.../repository/OrderRepository.java` | 修改 | 新增按物品ID查询订单的方法 |
| `backend/.../service/OrderService.java` | 修改 | 新增获取物品关联订单的方法 |
| `backend/.../controller/ItemController.java` | 修改 | 新增获取物品关联订单的接口 |
| `frontend/src/api/config/paths.js` | 修改 | 新增物品关联订单的路径 |
| `frontend/src/api/services/item.js` | 修改 | 新增获取物品关联订单的方法 |
| `frontend/src/views/user/Items.vue` | 修改 | 新增订单状态展示和操作按钮逻辑 |
| `frontend/src/styles/pages/user-items.css` | 修改 | 新增订单信息相关的样式 |

## 后续建议

1. **单元测试**：为新增的后端方法添加单元测试
2. **集成测试**：测试完整的订单流程
3. **UI 测试**：测试前端交互逻辑
4. **文档更新**：更新 API 文档
