import { ErrorCode, ErrorType, classifyError } from './errorTypes';
import ErrorHandler from './errorHandler';

const errorTypes = { ErrorCode, ErrorType, classifyError };

export { errorTypes, ErrorHandler };
